# Backend API URL
# URL của backend API (không có /api ở cuối, sẽ được thêm tự động)
VITE_API_URL=http://localhost:5000/api

# Admin Credentials
# Thông tin đăng nhập cho admin panel
# Thay đổi các giá trị này trong production!
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=admin123

# Notes:
# - VITE_ prefix là bắt buộc để Vite expose các biến này ra client-side
# - Các biến này sẽ được expose ra client-side, không nên đặt thông tin nhạy cảm
# - Để bảo mật tốt hơn, có thể implement authentication qua backend API

